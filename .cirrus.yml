container:
  image: golang:1.11

build_appindicator_task:
  apt_deps_script:
    - apt-get update
    - apt-get install -y libgtk-3-dev libappindicator3-dev
  main_build_script:
    - ./build.sh -a -v
  examples_build_script:
    - ./build.sh -v examples/simple/main.go